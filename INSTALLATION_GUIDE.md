# 🚀 คู่มือติดตั้งและใช้งานระบบโพสต์ข่าวอัตโนมัติ

## ⚠️ สถานะปัจจุบัน
- ✅ ไฟล์ทั้งหมดพร้อมแล้ว
- ❌ **Docker ยังไม่ได้ติดตั้ง** (ต้องติดตั้งก่อนใช้งาน)

---

## 📦 ขั้นตอนที่ 1: ติดตั้ง Docker Desktop

### สำหรับ Windows 10/11

1. **ดาวน์โหลด Docker Desktop**
   - ไปที่: https://www.docker.com/products/docker-desktop/
   - คลิก "Download for Windows"
   - ขนาดไฟล์ประมาณ 500 MB

2. **ติดตั้ง Docker Desktop**
   ```
   - Double-click "Docker Desktop Installer.exe"
   - เลือก "Use WSL 2 instead of Hyper-V" (แนะนำ)
   - คลิก "Ok" เพื่อติดตั้ง
   - รอจนเสร็จ (ประมาณ 5-10 นาที)
   ```

3. **Restart Windows**
   - หลังติดตั้งเสร็จ ต้อง restart Windows

4. **เปิด Docker Desktop**
   - หลัง restart, เปิด Docker Desktop
   - รอจน Docker Engine เริ่มทำงาน (มีไฟเขียว)
   - กด "Accept" License Agreement

5. **ตรวจสอบการติดตั้ง**
   ```powershell
   # เปิด PowerShell และรันคำสั่ง
   docker --version
   docker-compose --version
   ```
   
   ถ้าเห็นเลขเวอร์ชัน แสดงว่าติดตั้งสำเร็จ!

### ปัญหาที่อาจพบ

**ปัญหา: "WSL 2 installation is incomplete"**
```powershell
# แก้ไข: ติดตั้ง WSล 2 Update
# ดาวน์โหลดจาก: https://aka.ms/wsl2kernel
# หรือรันคำสั่ง:
wsl --install
```

**ปัญหา: "Hardware assisted virtualization"**
- เข้า BIOS เปิด Virtualization (VT-x หรือ AMD-V)
- วิธีเข้า BIOS: restart > กด F2/Del/F10 ระหว่าง boot

---

## 🚀 ขั้นตอนที่ 2: เริ่มใช้งานระบบ

### วิธีที่ 1: ใช้ไฟล์ start.bat (ง่ายที่สุด)

1. **Double-click ไฟล์ `start.bat`**
2. รอจนเห็นข้อความ "✅ เริ่มต้นสำเร็จ!"
3. เว็บเบราว์เซอร์จะเปิดอัตโนมัติไปที่ http://localhost:5678

### วิธีที่ 2: ใช้ PowerShell (สำหรับผู้ที่ต้องการควบคุมมากขึ้น)

```powershell
# 1. เปิด PowerShell ที่โฟลเดอร์โปรเจค
cd "g:\เทสทวิต"

# 2. เริ่มระบบ
docker-compose up -d

# 3. ตรวจสอบสถานะ
docker-compose ps

# 4. ดู logs
docker-compose logs -f
```

### เข้าใช้งาน n8n

1. เปิดเว็บเบราว์เซอร์ไปที่: http://localhost:5678
2. Login:
   - **Username:** `admin`
   - **Password:** `admin123`
3. **เปลี่ยนรหัสผ่าน** ใน Settings > Users

---

## 📊 ขั้นตอนที่ 3: ตั้งค่าบริการที่จำเป็น

### 3.1 Google Sheets

1. **สร้าง Spreadsheet**
   - ไปที่: https://sheets.google.com
   - คลิก "Blank" สร้าง spreadsheet ใหม่
   - ตั้งชื่อว่า "Test Flow YT"

2. **สร้างคอลัมน์**
   
   ในแถวแรก (Row 1), สร้างคอลัมน์ต่อไปนี้:
   
   | A: video_id | B: video_url | C: status | D: created_at |
   |-------------|--------------|-----------|---------------|
   
3. **เพิ่มข้อมูลทดสอบ** (Row 2):
   ```
   video_id: test001
   video_url: https://www.youtube.com/watch?v=dQw4w9WgXcQ
   status: pending
   created_at: 2024-11-21
   ```

4. **เชื่อมต่อกับ n8n**
   - ใน n8n, ไปที่ **Credentials** (มุมซ้ายล่าง)
   - คลิก **Add Credential**
   - เลือก **Google Sheets OAuth2 API**
   - คลิก **Connect my account**
   - ทำตาม OAuth flow (อนุญาต n8n เข้าถึง Google Sheets)
   - **Save**

### 3.2 OpenRouter (AI Service)

1. **สมัครสมาชิก OpenRouter**
   - ไปที่: https://openrouter.ai/
   - คลิก "Sign In" > เลือก Google/Discord
   - เข้าสู่ระบบ

2. **รับ API Key**
   - ไปที่: https://openrouter.ai/keys
   - คลิก "Create Key"
   - ตั้งชื่อ: "n8n News Bot"
   - **Copy API Key** (จะไม่แสดงอีก)

3. **ตั้งค่าใน n8n**
   - ใน n8n, ไปที่ **Credentials**
   - คลิก **Add Credential**
   - เลือก **OpenRouter API**
   - วาง API Key
   - **Save**

4. **เติมเงิน (ถ้าจำเป็น)**
   - ไปที่: https://openrouter.ai/credits
   - เติมเงินขั้นต่ำ $5 (ใช้งานได้นาน)

### 3.3 Facebook Graph API

1. **สร้าง Facebook App**
   - ไปที่: https://developers.facebook.com/
   - คลิก **My Apps** > **Create App**
   - เลือก **Business** type
   - ตั้งชื่อ App: "News Auto Poster"
   - **Create App**

2. **เพิ่ม Facebook Page**
   - ในหน้า App Dashboard
   - เลือก **Add Product** > **Facebook Login**
   - Settings > Valid OAuth Redirect URIs:
     ```
     https://www.facebook.com/connect/login_success.html
     ```
   - **Save Changes**

3. **รับ Access Token**
   - ไปที่: https://developers.facebook.com/tools/explorer/
   - เลือก App ของคุณ
   - เลือก **User Token** > กด **Generate Access Token**
   - เลือก permissions:
     - `pages_show_list`
     - `pages_read_engagement`
     - `pages_manage_posts`
     - `pages_manage_engagement`
   - **Copy Token**

4. **ขยายอายุ Token (สำคัญ!)**
   - ไปที่: https://developers.facebook.com/tools/debug/accesstoken/
   - วาง Token ของคุณ
   - คลิก **Extend Access Token**
   - **Copy** Token ใหม่ (อายุ 60 วัน)

5. **หา Page ID**
   - ไปที่ Facebook Page ของคุณ
   - คลิก **About**
   - ดูที่ **Page ID** (เลข 15 หลัก)

6. **ตั้งค่าใน n8n**
   - ใน n8n, ไปที่ **Credentials**
   - คลิก **Add Credential**
   - เลือก **Facebook Graph API**
   - ใส่:
     - **Access Token:** (token ที่ขยายอายุแล้ว)
   - **Save**

---

## 📥 ขั้นตอนที่ 4: Import Workflow

1. **ใน n8n, ไปที่ Workflows**
2. คลิก **Import from File**
3. เลือกไฟล์: `Flow 2 โพสต์ข่าว.json`
4. **Import**

5. **ตั้งค่า Credentials ใน Workflow**
   - เปิด workflow ที่ import มา
   - กดที่แต่ละ node ที่มีเครื่องหมาย ⚠️
   - เลือก credential ที่สร้างไว้
   - **Save**

6. **แก้ไข Google Sheet ID**
   - เปิด Google Sheet ของคุณ
   - ดู URL: `https://docs.google.com/spreadsheets/d/`**`YOUR_SHEET_ID`**`/edit`
   - Copy **YOUR_SHEET_ID**
   - ใน workflow, กดที่ node "Get Pending Job"
   - แก้ไข Document ID เป็น **YOUR_SHEET_ID**
   - ทำซ้ำกับ node "Mark as Done"
   - **Save**

7. **แก้ไข Facebook Page ID**
   - กดที่ node "Facebook Graph API"
   - แก้ไข **node** parameter เป็น Page ID ของคุณ
   - **Save**

8. **Activate Workflow**
   - คลิกสวิตช์ **Active** ที่มุมขวาบน
   - workflow จะเริ่มทำงานทุก 2 นาที

---

## 🎯 วิธีใช้งาน

### เพิ่มงานใหม่

1. **เปิด Google Sheets**
2. **เพิ่มแถวใหม่** พร้อมข้อมูล:
   ```
   video_id: (เว้นไว้หรือใส่ค่าอะไรก็ได้)
   video_url: https://www.youtube.com/watch?v=xxxxx
   status: pending
   created_at: 2024-11-21
   ```

3. **รอผลลัพธ์**
   - ภายใน 2 นาที, ระบบจะตรวจพบงานใหม่
   - ดาวน์โหลดวิดีโอ
   - วิเคราะห์ด้วย AI
   - โพสต์ไปยัง Facebook
   - เปลี่ยน status เป็น "done"

### ตรวจสอบการทำงาน

1. **ใน n8n**
   - ดูที่ **Executions** (ด้านซ้าย)
   - คลิกดู execution แต่ละครั้ง
   - ตรวจสอบว่าผ่านหรือ error

2. **ใน Google Sheets**
   - ดูว่า status เปลี่ยนเป็น "done" หรือไม่

3. **ใน Facebook Page**
   - ตรวจสอบว่าโพสต์ปรากฏหรือไม่

---

## 🛠️ คำสั่งที่ใช้บ่อย

### เริ่ม/หยุดระบบ

```powershell
# เริ่มระบบ
docker-compose up -d

# หยุดระบบ
docker-compose down

# Restart
docker-compose restart

# ดูสถานะ
docker-compose ps

# ดู logs
docker-compose logs -f

# ดู logs เฉพาะ n8n
docker-compose logs -f n8n

# ดู logs เฉพาะ API
docker-compose logs -f youtube-downloader-api
```

### ทำความสะอาด

```powershell
# หยุดและลบ containers
docker-compose down

# ลบ volumes (ข้อมูล n8n จะหายทั้งหมด!)
docker-compose down -v

# ลบไฟล์ดาวน์โหลด
Remove-Item -Recurse -Force downloads\*
```

---

## 🔍 Troubleshooting

### ปัญหา: Docker ไม่รัน

**สาเหตุ:**
- Docker Desktop ไม่เปิด
- Docker Engine ยังไม่พร้อม

**แก้ไข:**
1. เปิด Docker Desktop
2. รอจนไฟเป็นสีเขียว
3. ลองรันอีกครั้ง

### ปัญหา: n8n เข้าไม่ได้

**สาเหตุ:**
- Container ยังไม่พร้อม

**แก้ไข:**
```powershell
# ตรวจสอบสถานะ
docker-compose ps

# ดู logs
docker-compose logs -f n8n

# Restart
docker-compose restart n8n
```

### ปัญหา: วิดีโอดาวน์โหลดไม่ได้

**สาเหตุ:**
- URL ผิด
- วิดีโอเป็น private
- API service ล่ม

**แก้ไข:**
```powershell
# ตรวจสอบ API
curl http://localhost:8000/health

# ดู logs
docker-compose logs -f youtube-downloader-api

# Restart API
docker-compose restart youtube-downloader-api
```

### ปัญหา: Facebook โพสต์ไม่ได้

**สาเหตุ:**
- Access Token หมดอายุ
- Page ID ผิด
- ขาด permissions

**แก้ไข:**
1. ตรวจสอบ Token ที่: https://developers.facebook.com/tools/debug/accesstoken/
2. ขยายอายุ Token ใหม่
3. อัพเดท credential ใน n8n
4. ตรวจสอบ Page ID ถูกต้อง

### ปัญหา: AI ไม่ตอบ/Error

**สาเหตุ:**
- API Key ผิด
- เครดิตหมด
- Rate limit

**แก้ไข:**
1. ตรวจสอบ API Key ที่: https://openrouter.ai/keys
2. ตรวจสอบเครดิตที่: https://openrouter.ai/credits
3. เติมเงินถ้าจำเป็น

---

## 💰 ค่าใช้จ่ายโดยประมาณ

| บริการ | ค่าใช้จ่าย | หมายเหตุ |
|--------|-----------|----------|
| Docker | ฟรี | - |
| n8n | ฟรี | Self-hosted |
| Google Sheets | ฟรี | - |
| Facebook API | ฟรี | - |
| OpenRouter (AI) | ~$5-10/เดือน | ขึ้นกับการใช้งาน |

**โพสต์ 1 ครั้ง ใช้ AI ประมาณ $0.01-0.05**

ถ้าโพสต์วันละ 50 ครั้ง:
- ต่อวัน: $0.50-2.50
- ต่อเดือน: $15-75

---

## 📱 เคล็ดลับ

1. **ทดสอบก่อน** ด้วยวิดีโอ 1-2 อัน
2. **เช็ค quota** ของแต่ละ API บ่อยๆ
3. **Backup workflow** เป็นประจำ (Export เป็น JSON)
4. **Monitor logs** เพื่อดู error
5. **ตั้งเวลา** ให้เหมาะสม (ไม่ควรน้อยกว่า 2 นาที)

---

## 🎓 การเรียนรู้เพิ่มเติม

- **n8n Documentation:** https://docs.n8n.io/
- **yt-dlp:** https://github.com/yt-dlp/yt-dlp
- **Facebook Graph API:** https://developers.facebook.com/docs/graph-api
- **OpenRouter:** https://openrouter.ai/docs

---

## ✅ Checklist การติดตั้ง

- [ ] ติดตั้ง Docker Desktop
- [ ] Restart Windows
- [ ] เปิด Docker Desktop (ไฟเขียว)
- [ ] รัน `docker-compose up -d`
- [ ] เข้า n8n ได้ที่ http://localhost:5678
- [ ] สร้าง Google Sheets
- [ ] เชื่อมต่อ Google Sheets credential
- [ ] สมัคร OpenRouter และรับ API Key
- [ ] เชื่อมต่อ OpenRouter credential
- [ ] สร้าง Facebook App
- [ ] รับและขยายอายุ Access Token
- [ ] เชื่อมต่อ Facebook Graph API credential
- [ ] Import workflow
- [ ] แก้ไข Sheet ID และ Page ID
- [ ] Activate workflow
- [ ] ทดสอบด้วยงาน 1 อัน

---

**สร้างโดย:** Auto News Posting System  
**อัพเดท:** 2024-11-21 20:15 PM (GMT+7)  
**เวอร์ชัน:** 1.0.0

---

## 🆘 ต้องการความช่วยเหลือ?

หากมีปัญหา:
1. อ่าน Troubleshooting ด้านบน
2. ตรวจสอบ logs: `docker-compose logs -f`
3. ดูที่ Executions ใน n8n
4. ตรวจสอบ credentials ทั้งหมด

**Happy Posting! 🚀**

# 📝 สรุปโครงสร้างโปรเจค

## 📁 โครงสร้างไฟล์

```
g:\เทสทวิต\
│
├── 📄 ไฟล์สำคัญ
│   ├── Flow 2 โพสต์ข่าว.json          ← n8n Workflow (นำเข้าใน n8n)
│   ├── docker-compose.yml              ← การตั้งค่า Docker
│   ├── .env.example                    ← ตัวอย่าง environment variables
│   └── .gitignore                      ← Git ignore rules
│
├── 🎬 ไฟล์รัน (Batch Scripts)
│   ├── menu.bat                        ← 🌟 เมนูหลัก (เริ่มต้นที่นี่!)
│   ├── start.bat                       ← เริ่มระบบ
│   ├── check_system.bat                ← ตรวจสอบก่อนรัน
│   └── status.bat                      ← ดูสถานะระบบ
│
├── 📖 เอกสาร (Documentation)
│   ├── INSTALLATION_GUIDE.md           ← 🌟 คู่มือติดตั้งฉบับเต็ม
│   ├── README.md                       ← ภาพรวมโปรเจค
│   └── PROJECT_SUMMARY.md              ← ไฟล์นี้
│
├── 🐳 YouTube Downloader API
│   └── youtube-downloader-api/
│       ├── Dockerfile                  ← Docker image configuration
│       ├── main.py                     ← FastAPI application
│       └── requirements.txt            ← Python dependencies
│
├── 🐍 Scripts (Python)
│   └── scripts/
│       ├── fetch_youtube_shorts.py     ← ดึงวิดีโอจาก YouTube
│       └── process_video.py            ← ประมวลผลวิดีโอ
│
└── 📦 Folders (Auto-created)
    └── downloads/                       ← ที่เก็บวิดีโอที่ดาวน์โหลด
```

---

## 🚀 Quick Start (เริ่มใช้งานด่วน)

### ขั้นตอนที่ 1: ติดตั้ง Docker

1. ดาวน์โหลด: https://www.docker.com/products/docker-desktop/
2. ติดตั้งและ Restart Windows
3. เปิด Docker Desktop และรอจนไฟเป็นสีเขียว

### ขั้นตอนที่ 2: เริ่มใช้งาน

**วิธีที่ 1: ใช้เมนู (แนะนำ)**
```
Double-click: menu.bat
```

**วิธีที่ 2: รันตรงๆ**
```
Double-click: start.bat
```

### ขั้นตอนที่ 3: Import Workflow

1. เปิด http://localhost:5678
2. Login: `admin` / `admin123`
3. Import → `Flow 2 โพสต์ข่าว.json`

### ขั้นตอนที่ 4: ตั้งค่า Credentials

ต้องตั้งค่า 3 อย่าง:

1. **Google Sheets** → สำหรับจัดการคิวงาน
2. **OpenRouter** → สำหรับ AI (Gemini/GPT-4)
3. **Facebook Graph API** → สำหรับโพสต์

---

## 🎯 การใช้งาน

### เพิ่มงานใหม่

1. เปิด Google Sheets
2. เพิ่มข้อมูลในแถวใหม่:

| video_id | video_url | status | created_at |
|----------|-----------|--------|------------|
| - | https://youtube.com/watch?v=xxx | pending | 2024-11-21 |

3. รอ 2 นาที → ระบบจะประมวลผลอัตโนมัติ!

---

## 🔄 Flow การทำงาน

```
🕒 ตรวจสอบทุก 2 นาที
    ↓
📊 หางาน "pending" ใน Google Sheets
    ↓
📥 ดาวน์โหลดวิดีโอจาก YouTube
    ↓
🖼️  จับภาพหน้าจอ (screenshot)
    ↓
🤖 วิเคราะห์ด้วย AI (Gemini/GPT-4)
    ├─ สร้างแคปชั่นภาษาไทย
    └─ สร้างแฮชแท็ก
    ↓
📱 โพสต์ไปยัง Facebook Page
    ↓
✅ อัพเดท status → "done"
    ↓
🗑️  ลบไฟล์วิดีโอ
```

---

## 🛠️ คำสั่งที่ใช้บ่อย

| คำสั่ง | คำอธิบาย |
|--------|----------|
| `menu.bat` | เปิดเมนูหลัก |
| `start.bat` | เริ่มระบบ |
| `status.bat` | ดูสถานะ |
| `check_system.bat` | ตรวจสอบก่อนรัน |
| `docker-compose up -d` | เริ่มระบบ (CLI) |
| `docker-compose down` | หยุดระบบ |
| `docker-compose logs -f` | ดู logs |

---

## 🔗 URLs สำคัญ

| Service | URL | Username | Password |
|---------|-----|----------|----------|
| n8n | http://localhost:5678 | admin | admin123 |
| YouTube API | http://localhost:8000 | - | - |
| API Docs | http://localhost:8000/docs | - | - |

---

## ⚙️ Credentials ที่ต้องตั้งค่า

### 1. Google Sheets OAuth2
- **ที่มา:** Google Cloud Console
- **สิทธิ์:** Read/Write Sheets

### 2. OpenRouter API
- **ที่มา:** https://openrouter.ai/keys
- **ค่าใช้จ่าย:** ~$5-10/เดือน

### 3. Facebook Graph API
- **ที่มา:** https://developers.facebook.com/
- **ต้องการ:**
  - Access Token (ขยายอายุ)
  - Page ID
  - Permissions: `pages_manage_posts`, `pages_read_engagement`

---

## 📊 Nodes ใน Workflow

| Node | ประเภท | หน้าที่ |
|------|--------|---------|
| Every 2 Minutes | Trigger | เริ่มต้น workflow ทุก 2 นาที |
| Get Pending Job | Google Sheets | ดึงงาน status="pending" |
| IF Job Found | Condition | ตรวจว่ามีงานหรือไม่ |
| Download Video | HTTP Request | ดาวน์โหลดวิดีโอผ่าน FastAPI |
| Quote Creator | AI Agent | วิเคราะห์และสร้างแคปชั่น |
| Prepare FB Post Data | Code | จัดรูปแบบข้อมูลสำหรับโพสต์ |
| HTTP Request | HTTP | ดึงไฟล์วิดีโอ |
| Facebook Graph API | Facebook | โพสต์วิดีโอไปยัง Page |
| Mark as Done | Google Sheets | อัพเดท status="done" |
| Cleanup Video File | HTTP Request | ลบไฟล์วิดีโอ |

---

## 🐳 Docker Services

### n8n
- **Image:** n8nio/n8n:latest
- **Port:** 5678
- **Volume:** n8n_data (เก็บข้อมูล workflows, credentials)

### youtube-downloader-api
- **Build:** ./youtube-downloader-api
- **Port:** 8000
- **Volume:** ./downloads (เก็บวิดีโอชั่วคราว)

---

## 📈 การปรับแต่ง

### เปลี่ยนความถี่การรัน
- แก้ไข node "Every 2 Minutes"
- เปลี่ยน `minutesInterval` จาก 2 เป็นค่าที่ต้องการ

### เปลี่ยน AI Model
- แก้ไข node "Gemini" หรือ "Gemini1"
- เปลี่ยน `model` เป็น:
  - `google/gemini-2.5-flash-lite` (ถูก, เร็ว)
  - `openai/chatgpt-4o-latest` (แม่นยำ, แพง)
  - `meta-llama/llama-3.2-90b-vision` (เร็ว, ราคากลาง)

### เปลี่ยน Temperature (AI)
- แก้ไข `temperature` ใน AI nodes
- 0.0-1.0 → ยิ่งสูง ยิ่งสร้างสรรค์
- แนะนำ: 0.7-0.8

---

## 🔒 ความปลอดภัย

### ⚠️ สิ่งที่ต้องทำ

1. **เปลี่ยนรหัสผ่าน**
   - Login n8n → Settings → Users → Change Password
   - เปลี่ยนจาก `admin123` เป็นรหัสแข็งแรง

2. **อย่าแชร์**
   - API Keys
   - Access Tokens
   - Credentials

3. **Backup**
   - Export workflows เป็น JSON เป็นประจำ
   - เก็บไว้ใน Google Drive หรือที่ปลอดภัย

---

## 🐛 Troubleshooting พื้นฐาน

| ปัญหา | วิธีแก้ |
|-------|---------|
| Docker ไม่รัน | เปิด Docker Desktop > รอไฟเขียว |
| n8n เข้าไม่ได้ | `docker-compose restart n8n` |
| API ไม่ตอบสนอง | `docker-compose restart youtube-downloader-api` |
| วิดีโอดาวน์โหลดไม่ได้ | ตรวจสอบ URL, ดู logs API |
| Facebook โพสต์ไม่ได้ | ตรวจสอบ Token หมดอายุหรือไม่ |
| AI ไม่ตอบ | ตรวจเครดิต OpenRouter |

---

## 💰 ค่าใช้จ่าย

### ครั้งเดียว
- Docker Desktop: **ฟรี**
- n8n (Self-hosted): **ฟรี**

### รายเดือน
- Google Sheets: **ฟรี**
- Facebook API: **ฟรี**
- OpenRouter (AI): **$5-75/เดือน** (ขึ้นกับการใช้งาน)

**ประมาณการ:**
- 1 โพสต์ ≈ $0.01-0.05
- 50 โพสต์/วัน ≈ $15-75/เดือน

---

## 📚 เอกสารเพิ่มเติม

- [INSTALLATION_GUIDE.md](INSTALLATION_GUIDE.md) ← 🌟 คู่มือติดตั้งฉบับเต็ม
- [n8n Docs](https://docs.n8n.io/)
- [yt-dlp](https://github.com/yt-dlp/yt-dlp)
- [Facebook Graph API](https://developers.facebook.com/docs/graph-api)
- [OpenRouter](https://openrouter.ai/docs)

---

## ✅ Checklist

**การติดตั้ง:**
- [ ] ติดตั้ง Docker Desktop
- [ ] Restart Windows
- [ ] เปิด Docker Desktop (ไฟเขียว)
- [ ] รัน `menu.bat` หรือ `start.bat`
- [ ] เข้า n8n ได้

**การตั้งค่า:**
- [ ] สร้าง Google Sheets
- [ ] เชื่อมต่อ Google Sheets credential
- [ ] สมัคร OpenRouter + รับ API Key
- [ ] เชื่อมต่อ OpenRouter credential
- [ ] สร้าง Facebook App + รับ Token
- [ ] เชื่อมต่อ Facebook Graph API credential
- [ ] Import workflow จาก JSON
- [ ] แก้ไข Sheet ID และ Page ID
- [ ] Activate workflow

**การทดสอบ:**
- [ ] เพิ่มงานทดสอบใน Google Sheets
- [ ] รอ 2 นาที
- [ ] ตรวจสอบ Execution ใน n8n
- [ ] ตรวจสอบโพสต์ใน Facebook
- [ ] ตรวจสอบ status เปลี่ยนเป็น "done"

---

## 🎓 แนวคิดการใช้งาน

### Use Cases

1. **ข่าวประจำวัน**
   - เพิ่มลิงค์ข่าวใน Sheets
   - ระบบโพสต์อัตโนมัติทุก 2 นาที

2. **Content Curation**
   - รวบรวมวิดีโอจากหลายแหล่ง
   - AI สรุปและสร้างแคปชั่น
   - โพสต์แบบกระจาย

3. **Social Media Automation**
   - กำหนดเวลาโพสต์
   - Batch processing
   - Multi-platform (ขยายได้)

---

## 🔮 การขยายระบบ

### เพิ่ม Platforms
- Twitter/X → ใช้ Twitter API node
- Instagram → ใช้ Instagram Graph API
- TikTok → ใช้ TikTok API (ถ้ามี)

### เพิ่มความสามารถ
- ตรวจสอบ duplicate content
- แปลภาษาอัตโนมัติ
- ปรับขนาดวิดีโอให้เหมาะสมแต่ละแพลตฟอร์ม
- Analytics/Reporting

---

**Created:** 2024-11-21  
**Version:** 1.0.0  
**Author:** Auto News Posting System

---

💡 **เคล็ดลับ:** เริ่มจาก `menu.bat` เพื่อความสะดวก!

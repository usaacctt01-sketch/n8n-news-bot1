# 🐳 คู่มือติดตั้ง Docker Desktop สำหรับ Windows

## ⚠️ สิ่งที่ต้องมี

- Windows 10 64-bit: Pro, Enterprise, or Education (Build 16299 or later)
- หรือ Windows 11
- RAM อย่างน้อย 4GB (แนะนำ 8GB+)
- เปิดใช้งาน Hyper-V และ Containers Windows features

---

## 📥 ขั้นตอนการติดตั้ง

### 1. ดาวน์โหลด Docker Desktop

1. เปิดเว็บเบราว์เซอร์ไปที่:
   ```
   https://www.docker.com/products/docker-desktop/
   ```

2. คลิกปุ่ม **"Download for Windows"**

3. รอให้ไฟล์ดาวน์โหลดเสร็จ (ขนาดประมาณ 500 MB)

---

### 2. ติดตั้ง Docker Desktop

1. **เปิดไฟล์ที่ดาวน์โหลด:**
   - Double-click `Docker Desktop Installer.exe`

2. **Configuration:**
   - ✅ เลือก **"Use WSL 2 instead of Hyper-V"** (แนะนำ)
   - คลิก **"Ok"**

3. **รอการติดตั้ง:**
   - ระบบจะติดตั้งโปรแกรมและ dependencies
   - ใช้เวลาประมาณ 5-10 นาที

4. **เมื่อติดตั้งเสร็จ:**
   - จะมีข้อความขอให้ **Restart Windows**
   - คลิก **"Close and restart"**

---

### 3. หลัง Restart Windows

1. **เปิด Docker Desktop:**
   - หาโปรแกรม "Docker Desktop" ในเมนู Start
   - Double-click เพื่อเปิดโปรแกรม

2. **รอ Docker Engine เริ่มทำงาน:**
   ```
   เห็นข้อความ: "Docker Desktop is starting..."
   
   รอจน: มีไฟสีเขียว (🟢) ที่มุมล่างซ้าย
   
   หรือ: "Docker Desktop is running"
   ```

3. **Accept License Agreement:**
   - หากมีหน้าต่าง License Agreement
   - อ่านและคลิก **"Accept"**

4. **Skip Tutorial (ถ้ามี):**
   - สามารถ skip tutorial ได้
   - คลิก "Skip tutorial"

---

### 4. ตรวจสอบการติดตั้ง

#### วิธีที่ 1: ใช้ PowerShell

1. เปิด PowerShell:
   - กด `Win + X`
   - เลือก "Windows PowerShell" หรือ "Terminal"

2. พิมพ์คำสั่ง:
   ```powershell
   docker --version
   ```

3. ถ้าเห็นข้อความประมาณนี้ แสดงว่าติดตั้งสำเร็จ:
   ```
   Docker version 24.x.x, build xxxxxxx
   ```

4. ทดสอบเพิ่มเติม:
   ```powershell
   docker-compose --version
   ```

#### วิธีที่ 2: ใช้ check_system.bat

1. ไปที่โฟลเดอร์โปรเจค: `g:\เทสทวิต`
2. Double-click: **`check_system.bat`**
3. ถ้าเห็นเครื่องหมาย ✅ แสดงว่าพร้อมแล้ว!

---

## 🔧 ปัญหาที่อาจพบและวิธีแก้

### ❌ "WSL 2 installation is incomplete"

**สาเหตุ:** Windows Subsystem for Linux (WSL) ยังไม่ได้ติดตั้ง

**วิธีแก้:**

#### แบบที่ 1: ติดตั้งผ่าน PowerShell (ง่าย)
```powershell
# เปิด PowerShell แบบ Administrator
# (คลิกขวา > Run as administrator)

wsl --install
```

หลังจากนั้น **restart Windows** อีกครั้ง

#### แบบที่ 2: ดาวน์โหลด WSL 2 Update Package
1. ไปที่: https://aka.ms/wsl2kernel
2. ดาวน์โหลดและติดตั้ง "WSL2 Linux kernel update package"
3. Restart Windows

---

### ❌ "Hardware assisted virtualization and data execution protection must be enabled in the BIOS"

**สาเหตุ:** Virtualization ไม่ได้เปิดใน BIOS

**วิธีแก้:**

1. **Restart คอมพิวเตอร์**

2. **เข้า BIOS/UEFI:**
   - กดปุ่ม `F2`, `Del`, `F10`, หรือ `ESC` ระหว่าง boot
   - (ปุ่มที่ใช้ขึ้นกับรุ่นเมนบอร์ด)

3. **หา Virtualization Setting:**
   - Intel: มองหา **"Intel Virtualization Technology"** หรือ **"VT-x"**
   - AMD: มองหา **"AMD-V"** หรือ **"SVM Mode"**

4. **เปิดใช้งาน:**
   - เปลี่ยนจาก `Disabled` เป็น `Enabled`

5. **Save และ Exit:**
   - กด `F10` (หรือตามที่ BIOS แนะนำ)
   - เลือก "Save and Exit"

6. **Boot เข้า Windows ตามปกติ**

---

### ❌ "Docker Desktop requires Windows 10 Pro/Enterprise"

**สาเหตุ:** Windows 10 Home ไม่รองรับ Hyper-V

**วิธีแก้:**

#### ถ้าใช้ Windows 10 Home:
1. ใช้ **WSL 2 backend** แทน Hyper-V
2. ตอนติดตั้ง Docker Desktop ให้เลือก "Use WSL 2 instead of Hyper-V"

#### ถ้าต้องการ upgrade:
- Upgrade Windows 10 Home → Pro
- หรือ upgrade เป็น Windows 11

---

### ❌ Docker Desktop ไม่เปิด / ค้างที่ "Starting"

**วิธีแก้:**

1. **Restart Docker Desktop:**
   - คลิกขวาที่ Docker icon ใน System Tray
   - เลือก "Restart"

2. **Restart Docker Service:**
   ```powershell
   # เปิด PowerShell แบบ Administrator
   Restart-Service *docker*
   ```

3. **ลบและติดตั้งใหม่:**
   - Uninstall Docker Desktop
   - ลบโฟลเดอร์: `C:\ProgramData\Docker`
   - ลบโฟลเดอร์: `C:\Users\[YourUsername]\.docker`
   - ติดตั้ง Docker Desktop ใหม่

---

## ✅ ตรวจสอบว่า Docker พร้อมใช้งาน

### สัญญาณว่า Docker พร้อม:

1. **Docker Desktop แสดง:**
   - 🟢 ไฟสีเขียวที่มุมล่างซ้าย
   - หรือข้อความ "Docker Desktop is running"

2. **System Tray:**
   - เห็น Docker icon (🐋)
   - คลิกขวา > เห็นเมนู "Dashboard", "Settings" ฯลฯ

3. **PowerShell:**
   ```powershell
   docker --version
   docker-compose --version
   docker info
   ```
   ทั้ง 3 คำสั่งทำงานได้โดยไม่ error

---

## 🚀 พร้อมใช้งานแล้ว!

เมื่อ Docker Desktop พร้อมแล้ว:

1. กลับไปที่โฟลเดอร์: `g:\เทสทวิต`

2. เลือกวิธีเริ่มระบบ:
   ```
   วิธีที่ 1: Double-click menu.bat
   วิธีที่ 2: Double-click start.bat
   วิธีที่ 3: PowerShell > docker-compose up -d
   ```

3. เปิดเว็บเบราว์เซอร์:
   ```
   http://localhost:5678
   ```

---

## 📚 ทรัพยากรเพิ่มเติม

- **Docker Documentation:** https://docs.docker.com/
- **Docker Desktop for Windows:** https://docs.docker.com/desktop/windows/install/
- **WSL 2 Backend:** https://docs.docker.com/desktop/windows/wsl/
- **Troubleshooting:** https://docs.docker.com/desktop/troubleshoot/overview/

---

## 💡 Tips

### การใช้งาน Docker Desktop

1. **ไม่ต้องเปิดตลอด:**
   - Docker Desktop ไม่จำเป็นต้องเปิดตลอดเวลา
   - เปิดเฉพาะตอนต้องใช้งาน containers

2. **Settings ที่แนะนำ:**
   - Resources > Advanced:
     - CPUs: 2-4
     - Memory: 4-8 GB
   - General:
     - ✅ Start Docker Desktop when you log in (ถ้าใช้บ่อย)

3. **ดู Resource Usage:**
   - เปิด Docker Desktop
   - ไปที่ Dashboard
   - ดูการใช้ CPU, Memory ของแต่ละ container

---

**Happy Dockering! 🐳**

กลับไป: [INSTALLATION_GUIDE.md](INSTALLATION_GUIDE.md) | [START_HERE.txt](START_HERE.txt)
